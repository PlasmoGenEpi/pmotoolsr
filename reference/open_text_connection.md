# Open a text connection, optionally compressed

Open a text connection, optionally compressed

## Usage

``` r
open_text_connection(path, mode = c("rt", "wt"))
```

## Arguments

- path:

  File path.

- mode:

  Either `"rt"` or `"wt"`.

## Value

A connection object.
