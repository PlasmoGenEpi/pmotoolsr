# Pop name_key from each entry and set id_key from a (1-based) lookup

Pop name_key from each entry and set id_key from a (1-based) lookup

## Usage

``` r
.merge_replace_key(target_list, lookup, name_key, id_key)
```
