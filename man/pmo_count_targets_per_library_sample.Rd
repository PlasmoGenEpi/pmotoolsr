% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pmo_processor.R
\name{pmo_count_targets_per_library_sample}
\alias{pmo_count_targets_per_library_sample}
\title{Count the number of targets detected per library sample}
\usage{
pmo_count_targets_per_library_sample(pmo, min_reads = 0)
}
\arguments{
\item{pmo}{A \code{PortableMicrohaplotypeObject} or parsed PMO list.}

\item{min_reads}{Minimum summed reads for a target (across its detected
microhaplotypes) for that target to be counted.}
}
\value{
A tibble with columns \code{bioinformatics_run_id}, \code{library_sample_name},
\code{target_number}. Note: \code{bioinformatics_run_id} is 1-based; when a detected
set has no run id, a \verb{detected_microhaplotypes_count_idx_<n>} label is used.
}
\description{
Count the number of targets detected per library sample
}
\examples{
pmo <- read_pmo(
  system.file("extdata", "example_pmo.json.gz", package = "pmotoolsr"))
head(pmo_count_targets_per_library_sample(pmo))
}
