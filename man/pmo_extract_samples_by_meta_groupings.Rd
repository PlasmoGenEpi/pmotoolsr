% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pmo_processor.R
\name{pmo_extract_samples_by_meta_groupings}
\alias{pmo_extract_samples_by_meta_groupings}
\title{Extract specimens matching metadata groupings}
\usage{
pmo_extract_samples_by_meta_groupings(pmo, meta_fields_values)
}
\arguments{
\item{pmo}{A \code{PortableMicrohaplotypeObject} or parsed PMO list.}

\item{meta_fields_values}{File path or inline specification (see
\code{\link[=.parse_meta_groupings]{.parse_meta_groupings()}}).}
}
\value{
A list with \code{pmo} (a filtered PMO list) and \code{group_counts} (a tibble
with a \code{group} column, one column per field, and a \code{count} column).
}
\description{
Keeps the specimens (and their libraries/results) that satisfy any of the
supplied metadata groups, returning the filtered PMO and a per-group count
table. A specimen matches a group when it has every field in that group and
the field's value is among the group's listed values.
}
\examples{
p <- read_pmo(
  system.file("extdata", "example_full_pmo.json.gz", package = "pmotoolsr"))
res <- pmo_extract_samples_by_meta_groupings(p, "collection_country=Mozambique")
res$group_counts
}
