% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pmo_checker.R
\name{pmo_validate}
\alias{pmo_validate}
\title{Validate a PMO (structural and, by default, schema)}
\usage{
pmo_validate(pmo, schema_check = TRUE, schema = NULL, version = NULL)
}
\arguments{
\item{pmo}{A \code{PortableMicrohaplotypeObject} or parsed PMO list.}

\item{schema_check}{If \code{TRUE} (default), also run JSON Schema validation.}

\item{schema, version}{Passed through to \code{\link[=pmo_validate_jsonschema]{pmo_validate_jsonschema()}}.}
}
\value{
Invisibly \code{TRUE}; raises an error on the first failure.
}
\description{
Runs the always-available structural checks (required base fields plus the
generated R6 \verb{$validate()} constraints) and, when \code{schema_check = TRUE}, full
JSON Schema validation via \code{\link[=pmo_validate_jsonschema]{pmo_validate_jsonschema()}}.
}
\examples{
pmo <- read_pmo(
  system.file("extdata", "example_pmo.json.gz", package = "pmotoolsr"))
pmo_validate(pmo)
}
