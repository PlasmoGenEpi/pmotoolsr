% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pmo_updater.R
\name{pmo_merge_dicts_by_key}
\alias{pmo_merge_dicts_by_key}
\title{Merge two lists of dicts by a shared key field}
\usage{
pmo_merge_dicts_by_key(
  main_list,
  update_list,
  key_field,
  replace = FALSE,
  ignore_fields = NULL
)
}
\arguments{
\item{main_list}{The base list of named lists (source of truth).}

\item{update_list}{The list of named lists whose values are merged in.}

\item{key_field}{The field used to match records across the two lists.}

\item{replace}{If \code{TRUE}, update values overwrite existing fields; if \code{FALSE}
a conflicting field raises an error.}

\item{ignore_fields}{Optional field names never copied from \code{update_list}.}
}
\value{
A new list of merged named lists, in \code{main_list} order.
}
\description{
The first list is the base; the second provides updates applied on top,
matched by \code{key_field}. Both inputs are left unmodified.
}
\examples{
main <- list(list(name = "a", x = 1), list(name = "b", x = 2))
upd <- list(list(name = "a", y = 10))
pmo_merge_dicts_by_key(main, upd, "name")
}
