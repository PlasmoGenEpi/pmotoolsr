% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pmo_checker.R
\name{pmo_validate_jsonschema}
\alias{pmo_validate_jsonschema}
\title{Validate a PMO against the JSON Schema}
\usage{
pmo_validate_jsonschema(
  pmo,
  schema = NULL,
  version = NULL,
  engine = c("ajv", "imjv"),
  error = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{pmo}{A \code{PortableMicrohaplotypeObject}, parsed PMO list, file path, or
JSON string.}

\item{schema}{Optional schema (file path, JSON string, or parsed list). If
\code{NULL}, the bundled schema for \code{version} is used.}

\item{version}{Optional schema version to validate against (defaults to
\code{\link[=pmo_schema_version]{pmo_schema_version()}}). Lets you validate a raw PMO against a different
schema version than the one the R6 classes were generated from.}

\item{engine}{jsonvalidate engine, \code{"ajv"} (default) or \code{"imjv"}.}

\item{error}{If \code{TRUE} (default), raise an error when validation fails;
otherwise return the logical result (with an \code{errors} attribute).}

\item{verbose}{Passed to the validator to collect error details.}
}
\value{
Invisibly \code{TRUE} when valid (or the logical result when
\code{error = FALSE}).
}
\description{
Performs full JSON Schema validation using \pkg{jsonvalidate} (ajv engine).
The PMO is converted to its on-disk (0-based) JSON form before validation.
}
\examples{
p <- read_pmo(
  system.file("extdata", "example_full_pmo.json.gz", package = "pmotoolsr"))
pmo_validate_jsonschema(p)
}
